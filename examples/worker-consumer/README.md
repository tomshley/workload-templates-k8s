# Worker consumer

Minimal local composition of the non-HTTP worker workload and ServiceAccount
component. CI renders this example to verify that name-prefix reference
rewrites remain valid for downstream consumers.
